services:
  frontend:
    image: loicpincon/app-spa-intex-front:latest
    ports:
      - "80:3000"
    networks:
      spa-intex-app-network:
  database:
    image: mongo:latest
    networks:
      spa-intex-app-network:
  mosquito:
    container_name: mosquito
    image: eclipse-mosquitto
    ports:
      - 1883:1883
      - 9001:9001
    networks:
      spa-intex-app-network:
networks:
  spa-intex-app-network: